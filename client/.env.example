# Variables d'environnement pour le client (optionnel)
# Ces variables seront utilisées par Vite

# URL de l'API (par défaut utilise le proxy Vite)
VITE_API_URL=http://localhost:5000/api

# Configuration pour les API externes
VITE_SCRYFALL_API_URL=https://api.scryfall.com
